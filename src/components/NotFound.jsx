import "../scss/NotFound.scss";

function NotFound() {
    return (  
        <section className={`not-found container p-3 mt-5`}>
            <h1>Page not found</h1>
        </section>
    );
}

export default NotFound;